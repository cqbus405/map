import request from 'request'

class Baidu {

	constructor() {
	}

	let getCordinate = (url, query, region, cityLimit, location, coordType, retCoordtype, output, ak, sn, timestamp, callback) => {
		request(url, (error, response, body) => {
			
		})
	}
}